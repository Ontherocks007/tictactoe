import React from 'react';

const Jugada = (props) =>{
    return(
    <div className="{props.valor.winningCombinations}" onClick={props.onClickwinningCombinations}>
        </div>
    )
}

export default Jugada;